# Find the longest path in a graph from a given set of vertices and their coordinates

n = int(input("Number of vertices: "))
vertices = []
i = 0
while i < n:
    vertices.append(int(input()))
    i += 1
print(vertices)
